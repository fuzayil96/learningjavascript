<%- include('includes/navigation.ejs') %>
</head>
<body>
    <%- include('includes/head') %>
    <h1>Users</h1>
    <main>
        <% if (prods.length > 0) { %>
            <div class="grid">
                <% for (let product of prods) { %>
                    
                            <h1><%= product.title %></h1>
                        
                    
                <% } %>
            </div>
        <% } else { %>
            <h1>No Products Found!</h1>
        <% } %>
    </main>
</body>
</html>